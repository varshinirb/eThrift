# to create a virtual env
python3 -m venv env(can be any name) 

# to activate the virtual env
source env/bin/activate


# To runserver and to close server
python manage.py runserver and ctrl+c to close the server